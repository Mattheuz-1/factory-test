# factory-test
Generated by [Rojo](https://github.com/rojo-rbx/rojo) 7.7.0.

## Development

Run all mechanical checks and a temporary Rojo build:

```powershell
pwsh -NoProfile -File scripts/Build-Check.ps1
```

To sync the filesystem into Studio:

```powershell
rojo serve default.project.json
```

Persistent source lives in Git; Studio is used only for runtime testing. See `AGENTS.md`, `ARCHITECTURE.md`, and `docs/STATE.md` before an experiment.

To create a named local place artifact when needed:

```powershell
rojo build default.project.json -o factory-test.rbxlx
```
