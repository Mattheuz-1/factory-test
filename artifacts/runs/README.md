# Run artifacts

Store small, structured run evidence here (prefer JSON or Markdown): contract id/version, inputs, telemetry, console findings, artifact hash, and verdict. Do not commit large screenshots or place binaries by default.
